{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "^(apply_patch|apply_patch_freeform|Edit|Write)$",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import pathlib, runpy, subprocess, sys; root = pathlib.Path(subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], text=True).strip()); hook_dir = root / '.codex' / 'hooks'; sys.path.insert(0, str(hook_dir)); runpy.run_path(str(hook_dir / sys.argv[1]), run_name='__main__')\" format_edited_files.py",
            "timeout": 120,
            "statusMessage": "Formatting edited files"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import pathlib, runpy, subprocess, sys; root = pathlib.Path(subprocess.check_output(['git', 'rev-parse', '--show-toplevel'], text=True).strip()); hook_dir = root / '.codex' / 'hooks'; sys.path.insert(0, str(hook_dir)); runpy.run_path(str(hook_dir / sys.argv[1]), run_name='__main__')\" stop_checks.py",
            "timeout": 900,
            "statusMessage": "Running edited-area checks and tests"
          }
        ]
      }
    ]
  }
}
