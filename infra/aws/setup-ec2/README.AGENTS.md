# setup-ec2

- Purpose: create the EC2 instance inside PROD VPC and create its instance profile, GitHub Actions roles, ECR image registries, and setup for SSH from the admin IP
- Executed by: `infra/aws/setup-ec2/setup-ec2.sh`
- Not idempotent
